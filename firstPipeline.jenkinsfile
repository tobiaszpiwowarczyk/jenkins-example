pipeline {
  agent "any"

  stages {
    stage("Print some text") {
      steps {
        script {
          sh 'echo "Hello from first pipeline"'
        }
      }
    }
  }

  post {
    success {
      echo "Running second pipeline"
      build job: 'secondPipeline', parameters: []
    }
  }
}
